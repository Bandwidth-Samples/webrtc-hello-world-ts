{
  "name": "webrtc-hello-world",
  "version": "1.0.0",
  "description": "",
  "main": "./build/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "npm run build && node ./build/server.js",
    "postinstall": "cd frontend && npm install && npm run build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/express": "^4.17.2",
    "@types/jwt-decode": "^2.2.1",
    "axios": "^0.19.2",
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jwt-decode": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^3.7.5"
  }
}
